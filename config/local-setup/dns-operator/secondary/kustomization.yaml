resources:
  - ../../../deploy/local

patches:
  - patch: |-
      - op: add
        path: /spec/template/spec/containers/0/args/-
        value: --delegation-role=secondary
    target:
      kind: Deployment
