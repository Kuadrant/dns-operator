name: Version Gate

on:
  pull_request:
    branches:
      - "release-**"
    paths:
      - "release.yaml"

permissions:
  contents: read

jobs:
  validate-release-yaml:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/install-yq

      - name: Validate release.yaml
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          .github/scripts/validate-release-yaml.sh "${{ github.base_ref }}" "Kuadrant"
