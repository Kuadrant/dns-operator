apiVersion: v1
stringData:
  AWS_ACCESS_KEY_ID: "${awsAccessKey}"
  AWS_REGION: ""
  AWS_SECRET_ACCESS_KEY: "${awsSecretKey}"
kind: Secret
metadata:
  name: aws-credentials
  namespace: ${testDeletableNamespace}
type: kuadrant.io/aws